name: Diagnose Lucifera

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  diagnose:
    if: github.head_ref == 'implement-lucifera'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout feature branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}

      - name: Insert Lucifera data
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path
          workflow = Path('.github/workflows/implement-lucifera.yml').read_text(encoding='utf-8')
          start = "          python - <<'PY'\n"
          end = "\n          PY"
          block = workflow.split(start, 1)[1].split(end, 1)[0]
          code = '\n'.join(line[10:] if line.startswith('          ') else line for line in block.splitlines())
          exec(compile(code, 'embedded-lucifera-implementation.py', 'exec'))
          PY

      - name: Run Lucifera test
        shell: bash
        run: |
          set +e
          node tests/lucifera-tests.js > lucifera-test.log 2>&1
          status=$?
          cat lucifera-test.log
          echo "$status" > lucifera-test-status.txt
          exit 0

      - name: Upload diagnostic log
        uses: actions/upload-artifact@v4
        with:
          name: lucifera-diagnostic
          path: |
            lucifera-test.log
            lucifera-test-status.txt
          if-no-files-found: error
